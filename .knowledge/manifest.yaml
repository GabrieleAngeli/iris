# Codebase Knowledge Layer — manifest
#
# This file is regenerated by the (future) knowledge-layer pipeline. Hand-maintained
# through Phase 4 (Hardening) — CI now runs the two scripts below on every PR/push
# to main (see .github/workflows/knowledge-layer.yml), but nothing regenerates this
# manifest automatically yet. Concept coverage is still Phase 2's single domain
# (Applications catalog) — see "scope" below.

schema_version: "1.0"
generated_at: "2026-09-07T12:35:13Z"
source_commit: "f71240c181f86eb8c7757c0458d0807471ff4984"
source_branch: "feature/deployments-validation"
generator_version: "0.1.0-poc"
default_branch: "main"
repository: "https://github.com/GabrieleAngeli/iris.git"
validation_status: "passed"   # 8/8 concept files, 0 errors, 0 warnings — see validation-report.md
drift_status: "clean"          # 0 drifted / 8 clean / 0 unverifiable — see drift-report.md
# Caveats:
# - "passed"/"clean" reflect the PoC-grade regex validator and git-log-based drift
#   detector in .knowledge/tools/*.ps1, not a full YAML/schema validator or a
#   semantic correctness check — see MAINTENANCE.md and each script's own header.
# - Both reports are regenerated on every run (locally or in CI) — trust the
#   committed validation-report.md/drift-report.md only as of the commit they were
#   generated at, not as a standing guarantee for later commits.

# Scope of this pass — Phase 2 PoC covers one domain only. Everything else in
# the repository (Access/Governance, Infrastructure, Deployments/Validation,
# Setup/Mail, Audit, the Iris.App client) is intentionally NOT represented yet.
# See .knowledge/index.md "Coverage" for the honest list of what exists vs what
# is documented here.
scope:
  domains_covered:
    - applications
  domains_known_but_undocumented:
    - access-governance
    - infrastructure
    - deployments-validation
    - setup-mail
    - audit
    - iris-app-client
