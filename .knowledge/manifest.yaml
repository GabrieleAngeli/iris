# Codebase Knowledge Layer — manifest
#
# This file is regenerated by the (future) knowledge-layer pipeline. During the
# Phase 2 proof of concept it is maintained by hand and reflects the state of a
# single incremental pass over one domain (Applications catalog). Do not treat
# validation_status as a repo-wide guarantee until Phase 4 wires this into CI.

schema_version: "1.0"
generated_at: "2026-09-07T11:54:15Z"
source_commit: "7e011bf0dd7c5a152a612554ef523e6081923c50"
source_branch: "feature/deployments-validation"
generator_version: "0.1.0-poc"
default_branch: "main"
repository: "https://github.com/GabrieleAngeli/iris.git"
validation_status: "passed"   # 8/8 concept files, 0 errors, 0 warnings — see validation-report.md
# Caveat: "passed" reflects the PoC-grade regex validator in
# .knowledge/tools/Validate-Knowledge.ps1, not a full YAML/schema validator —
# see that script's header before treating this as a strong guarantee.

# Scope of this pass — Phase 2 PoC covers one domain only. Everything else in
# the repository (Access/Governance, Infrastructure, Deployments/Validation,
# Setup/Mail, Audit, the Iris.App client) is intentionally NOT represented yet.
# See .knowledge/index.md "Coverage" for the honest list of what exists vs what
# is documented here.
scope:
  domains_covered:
    - applications
  domains_known_but_undocumented:
    - access-governance
    - infrastructure
    - deployments-validation
    - setup-mail
    - audit
    - iris-app-client
